# Scoring Tradeoffs

현재 scoring 설계에서 인지된 tradeoff와 미래 재검토 트리거를 기록한다.

## anchorFill vs consecutive 편향 (Q≥5)

### 배경

`consecutive` 보너스는 각 maximal consecutive run 길이 L에 대해 `(L-1)² × cons` 로 가산된다 (제곱). 이는 `anchorFill`의 Σ(atoms²) 철학과 대칭이고, 4-run이 2+2 run을 강하게 이기도록 한다.

예시 (기본 가중치):
- 쿼리 `"ㅇㅎㅈㅇ"` × target `"제1목 은행재원 ..."` [4,5,6,7] = 4-run → cons 180점
- 쿼리 `"ㅇㅎㅈㅇ"` × target `"외화 및 장외..."` [0,1,5,6] = 2+2 → cons 40점
- 4-run이 명확히 우위.

### 편향

완전 매치(한 anchor에 atom 몰아넣기)와 2+1 spill chain(atom을 인접 anchor로 나눠 길게 이어붙이기)을 비교하면, 긴 쿼리에서 spread가 완전 매치를 이길 수 있다.

전형적 3-atom 음절(예: ㅇㅓㅂ) × Q개 grapheme 쿼리, 기본 가중치:

| Q | 완전 매치 (anchorFill Q·9·50 + cons (Q-1)²·20) | 2+1 spread chain (anchorFill Q·5·50 + cons (2Q-1)²·20) | 승자 |
|---|---|---|---|
| 2 | 900 + 20 = 920 | 500 + 180 = 680 | 완전 +240 |
| 3 | 1350 + 80 = 1430 | 750 + 500 = 1250 | 완전 +180 |
| 4 | 1800 + 180 = 1980 | 1000 + 980 = 1980 | **TIE** |
| 5 | 2250 + 320 = 2570 | 1250 + 1620 = 2870 | spread +300 |
| 6 | 2700 + 500 = 3200 | 1500 + 2420 = 3920 | spread +720 |

수식:
- 완전: `9·Q·50 + (Q-1)²·20`
- spread: `5·Q·50 + (2Q-1)²·20`
- spread − 완전 = `60·Q·(Q-4)`

Q>4일 때 spread가 수식상 완전 매치를 이긴다.

### 실용적 심각성 (현재 판단: 낮음)

2+1 spill chain이 자연스럽게 발생하려면 target이 "가나가나가나…" 식으로 **모든 grapheme이 '2-atom anchor + 1-atom 다음 anchor 초성'으로 연결되는 병리적 구조**여야 한다. 한국어 실제 단어/문구에서는 드물다. command palette 용도로 Q≥5인 쿼리 자체도 흔치 않다.

따라서 현재 상태로 커밋했고, 실사용에서 회귀가 발견되기 전까지는 원 설계를 유지한다.

### 완화 옵션 (필요 시 도입)

1. **`CONSECUTIVE` 가중치 하향** (20 → 10~15): 교차점을 뒤로 민다. 단순하지만 정상 케이스의 4-run 우대도 약해짐.
2. **Sub-quadratic 스케일** (`(L-1)^1.5` 혹은 `(L-1)·log L`): 긴 run 우대는 유지하되 증가율 둔화.
3. **Per-run cap** (`min((L-1)², C_max)`, 예: C_max=9): L≥4부터 이득이 고정. Q=4 케이스(ㅇㅎㅈㅇ 4-run)는 그대로 해결되고 Q≥5 병리 케이스만 완화.

추천은 **3번(cap)** — 현재 의도를 살리면서 병리 케이스만 차단.

### 재검토 트리거

- 실사용자 쿼리에서 "완전 매치 대신 spread가 상위에 온다"는 회귀 보고.
- Q≥5 쿼리가 주 사용 패턴인 새 use case 등장.
- 새 target 도메인(예: 긴 영문 식별자)에서 유사 편향 관찰.
