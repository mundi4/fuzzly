// const segmenter = new Intl.Segmenter("und", { granularity: "grapheme" });

// export function splitGraphemesSegmenter(str: string): string[] {
//     return Array.from(segmenter.segment(str), s => s.segment);
// }